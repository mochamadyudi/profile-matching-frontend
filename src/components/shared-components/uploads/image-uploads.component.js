import React, { Component } from 'react';
import { connect } from 'react-redux';

function mapStateToProps(state) {
 return {

 };
}

function ImageUploadsComponent()  {
  return (
   <div>

   </div>
  );
}

export default connect(
 mapStateToProps,
)(ImageUploadsComponent);